<?xml version="1.0" encoding="utf-8"?>
<resources>
    <style name="Theme.EventGaze.Splash" parent="Theme.Material3.Dark.NoActionBar">
        <item name="windowSplashScreenBackground">@color/black</item>
        <!-- Set an image or logo for the splash screen -->
        <item name="windowSplashScreenAnimatedIcon">@drawable/ic_launcher_foreground</item>
        <!-- Optional: Control how long the splash logo stays before the app screen appears -->
        <item name="postSplashScreenTheme">@style/Theme.EventGaze</item>
    </style>

    <style name="Theme.EventGaze" parent="android:Theme.Material.NoActionBar"/>
</resources>